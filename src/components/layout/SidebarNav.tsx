import React from 'react';

interface SidebarNavProps {
  currentPath: string;
  onNavigate: (path: string) => void;
}

export const SidebarNav: React.FC<SidebarNavProps> = () => {
  // Navigation is now streamlined into the top floating glass navbar
  return null;
};
